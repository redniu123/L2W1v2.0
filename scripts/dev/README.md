# Deprecated Development Scripts

These scripts are deprecated dev, smoke, or provider-check helpers moved out of the active `scripts/` root during cleanup Stage 9.

They are kept only for traceability. Do not run them during cleanup; several can load models, read provider configuration, or call APIs.
